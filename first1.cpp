#include<stdio.h>
int main()
{
    int n;
 printf("Enter the no");
 scanf("%d",&n);
 
 if(n>0)
 {
 printf("the no is positvie");
}
 if(n==0)
 {
   printf("the no is zero");
}
if(n<0)
{
       printf("the no is negative");
       }
       return 0;
       }
